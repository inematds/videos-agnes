# videos-agnes
História → filme animado narrado via Agnes AI (US$ 0). Ver a skill `videos-agnes`.
`python3 rodar.py <nome>` (spec em `historias/<nome>.py`). Saída em `~/projetos/output/videos-agnes/`.
